{
  "_comment": "Copy this file to testmatrix.json and fill in your provider details. The _comment field is ignored by the parser — it is a documentation-only convention.",

  "filter": "",

  "providers": [
    {
      "name": "AWS S3",
      "config": ".env.aws"
    },
    {
      "name": "Wasabi",
      "config": ".env.wasabi"
    },
    {
      "name": "Backblaze B2",
      "config": ".env.backblaze"
    },
    {
      "name": "Impossible Cloud",
      "config": ".env.impossible"
    }
  ]
}
